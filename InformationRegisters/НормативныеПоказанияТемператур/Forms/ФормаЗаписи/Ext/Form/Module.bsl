
&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	Если Запись.ТемператураОт> Запись.ТемператураДо Тогда
		ТемператураДо =  Запись.ТемператураДо;
		Запись.ТемператураДо = Запись.ТемператураОт;
		Запись.ТемператураОт = ТемператураДо;
	КонецЕсли;
КонецПроцедуры
