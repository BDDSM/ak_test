
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	СписокВыбораТипаСсылки = Элементы.ТипСсылки.СписокВыбора;
	ТипыИзмерения = Метаданные.РегистрыСведений.КабинетПоставщикаОтбор.Измерения.Ссылка.Тип.Типы();
	Для Каждого Тип Из ТипыИзмерения Цикл
		СписокВыбораТипаСсылки.Добавить(XMLТип(Тип).ИмяТипа, Строка(Тип));
	КонецЦикла;
	
	СписокВыбораТипаСсылки.СортироватьПоПредставлению();
	ТипСсылки = СписокВыбораТипаСсылки[0].Значение;
	ТипСсылкиПриИзмененииНаСервере();
	
КонецПроцедуры

&НаСервере
Процедура ТипСсылкиПриИзмененииНаСервере()
	ОбщегоНазначенияКлиентСервер.УстановитьНедоступныйЭлементОтбора(
		Список.Отбор, "ТипСсылки", СериализаторXDTO.ИзXMLТипа(Новый ТипДанныхXML(ТипСсылки, "http://v8.1c.ru/8.1/data/enterprise/current-config")));
КонецПроцедуры

&НаКлиенте
Процедура ТипСсылкиПриИзменении(Элемент)
	ТипСсылкиПриИзмененииНаСервере();
КонецПроцедуры

//+++АК SHEP 2018.03.26 ИП-00017032: добавлен регистр сведений и форма списка
