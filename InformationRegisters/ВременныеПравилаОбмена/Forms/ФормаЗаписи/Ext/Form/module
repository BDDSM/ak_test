
Процедура ПередЗаписью(Отказ)
	
	Если РегистрСведенийМенеджерЗаписи.ДействуютДо > КонецДня(ТекущаяДата()) Тогда
		Ответ = Вопрос("Временные правила лучше ставить с периодом действия до конца дня, если это не выходные, т.к. с утра будет обновление, а база будет работать по временным. Продолжить?", РежимДиалогаВопрос.ДаНет);
		Если Ответ = КодВозвратаДиалога.Нет Тогда
			Отказ = Истина;
		КонецЕсли;
	КонецЕсли;
	
	РегистрСведенийМенеджерЗаписи.Правила = Новый ХранилищеЗначения(ЭлементыФормы.ПолеТекстовогоДокумента.ПолучитьТекст());
	
КонецПроцедуры

Процедура ПриОткрытии()
	
	ЭтаФорма.ТолькоПросмотр = Ложь;
	
	ЭлементыФормы.ПолеТекстовогоДокумента.УстановитьТекст(РегистрСведенийМенеджерЗаписи.Правила.Получить());
		
КонецПроцедуры
