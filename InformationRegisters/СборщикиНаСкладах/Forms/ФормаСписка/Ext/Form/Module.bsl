
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	//+++АК SHEP 2018.04.06 ИП-00018324
	Если Параметры.РежимВыбора = Истина Тогда
		Элементы.Список.РежимВыбора = Истина;
	КонецЕсли;
	//---АК SHEP 2018.04.06
	
КонецПроцедуры

&НаСервере
Функция ПечатьБейджейСервер(Сборщики)
	
	Возврат Обработки.ГрафикРаботыПродавцов_ТЗ.СформироватьТабличныйДокумент_Сборщики(Сборщики);
	
КонецФункции

&НаКлиенте
Процедура ПечатьБейджей(Команда)
	
	МассивФЛ = Новый Массив();
	Для Каждого СтрокаСписка Из Элементы.Список.ВыделенныеСтроки Цикл
		ДанныеСтроки = Элементы.Список.ДанныеСтроки(СтрокаСписка);
		МассивФЛ.Добавить(ДанныеСтроки.Сборщик);
	КонецЦикла;	
	ТабДок = ПечатьБейджейСервер(МассивФЛ);
	ТабДок.Показать("Бэйджи");
	
КонецПроцедуры
