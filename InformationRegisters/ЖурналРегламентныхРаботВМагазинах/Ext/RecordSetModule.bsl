
Процедура ПередЗаписью(Отказ, Замещение)
	
	Для Каждого Запись Из ЭтотОбъект Цикл
		Если НЕ ЗначениеЗаполнено(Запись.ИдентификаторРаботы) Тогда
			Запись.ИдентификаторРаботы = Новый УникальныйИдентификатор();	
		КонецЕсли;
		Если НЕ ЗначениеЗаполнено(Запись.ИдентификаторРаботыСтрока) Тогда
			Запись.ИдентификаторРаботыСтрока = Строка(Запись.ИдентификаторРаботы);	
		КонецЕсли;
	КонецЦикла;  
	
КонецПроцедуры
