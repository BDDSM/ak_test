
&НаСервереБезКонтекста
Функция ПолучитьИДНом(мНоменклатура)
	
	Возврат мНоменклатура.id_tov;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьИДКонтр(мКонтрагент)
	
	Возврат мКонтрагент.ИД;
	
КонецФункции


&НаКлиенте
Процедура НоменклатураПриИзменении(Элемент)
	
	Запись.id_tov = ПолучитьИДНом(Запись.Номенклатура);
	
КонецПроцедуры

&НаКлиенте
Процедура КонтрагентПриИзменении(Элемент)
	
	Запись.id_kontr = ПолучитьИДКонтр(Запись.Контрагент);
	
КонецПроцедуры
