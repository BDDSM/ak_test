
//+++АК SHEP 2018.10.03 ИП-00018753.03 +форма
 
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если ЗначениеЗаполнено(Запись.id_OK) Тогда
		
		ЗаписьОбъект = РеквизитФормыВЗначение("Запись");
		Если ЗаписьОбъект.Выбран() Тогда
			ТолькоПросмотр = Истина;
		Иначе
			Элементы.Период.ТолькоПросмотр = Истина;
			Элементы.id_OK.Видимость = Ложь;
			Элементы.GUID_Загрузки.Видимость = Ложь;
			Элементы.ДатаДок.Видимость = Ложь;
			Запись.ЧейОтвет = "Ответ техподдержки";
			Элементы.ЧейОтвет.ТолькоПросмотр = Истина;
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры
