&НаКлиенте
Процедура глОбработкаОжиданияДляОповещения() Экспорт
	
	глДопПараметрыПользователя = РаботаСОбщимиПеременными.ПолучитьЗначениеПеременной("глДопПараметрыПользователя");
	
	Если глДопПараметрыПользователя = Неопределено Тогда
		Возврат;
	КонецЕсли;	
	
	//mind не соблюден контекст доступности на клиенте
	//luza этот код был, свой удалил
	Если глДопПараметрыПользователя.ЕстьРольОбработкаЗвонков Тогда
		Если Телеграм.ЕстьНеобработанныеЗвонки() Тогда
			//Сообщить("!");
			//Оп = Новый ОписаниеОповещения("ПоказатьРеестрЗвонков", ОбщегоНазначенияКлиент,);
			ПоказатьОповещениеПользователя("Есть новые заказы звонков!",ПолучитьНавигационнуюСсылку(ПолучитьФорму("Обработка.АК_РеестрОбратныхЗвонков.Форма.ФормаСписка")),"Открыть реестр обратных звонков",БиблиотекаКартинок.БольшойТелефон,СтатусОповещенияПользователя.Важное);
		КонецЕсли;		
	КонецЕсли;
	
	//++ luza 27.09.2017
	Если глДопПараметрыПользователя.ЕстьРольОператорСклада Тогда
		Если Телеграм.ЕстьОповещенияПоСкладу() Тогда
			ПоказатьОповещениеПользователя("Есть ошибки в распознавании документов!",ПолучитьНавигационнуюСсылку(ПолучитьФорму("Обработка.АК_ИнформацияОРаспознаванииДокументовСклад.Форма.ФормаСписка")),"Открыть список документов с ошибками",БиблиотекаКартинок.Сканирование,СтатусОповещенияПользователя.Важное);
		КонецЕсли;	
		          		
		//++ АК luza ИП-00018473 20.04.2018
		Если РегламентныеЗаданияСервер.ЕстьОповещениеОПроблемахСОбменомКонтурEDIДляСклада() Тогда
			//ОбщегоНазначенияКлиент.ПоказатьОповещениеПоСкладу();			
			Оп = Новый ОписаниеОповещения("ОткрытьФормуСпискаРасходныйОрдерСклад", ОбщегоНазначенияКлиент);
			ПоказатьОповещениеПользователя("Есть проблемы в документах EDI", Оп, "Открыть список документов" ,БиблиотекаКартинок.ЗначокСклад16 ,СтатусОповещенияПользователя.Важное);
		КонецЕсли;	
		//-- АК luza ИП-00018473 20.04.2018
		 
	КонецЕсли;	
	//-- luza 27.09.2017
		
	 
КонецПроцедуры

&НаКлиенте
Процедура глПоказатьРеестрЗвонков() Экспорт

	ОткрытьФорму("Обработка.АК_РеестрОбратныхЗвонков.Форма.ФормаСписка");

КонецПроцедуры


