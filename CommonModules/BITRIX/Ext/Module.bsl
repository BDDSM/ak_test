
//
//
Функция GetUserByIP(IP) Экспорт
	
	//
	ТекстJSON = "NULL";
	
	//
	ТЗ = "ВЫБРАТЬ РАЗЛИЧНЫЕ ПЕРВЫЕ 1
	     |	Таблица.Ссылка.id_TT КАК ID,
	     |	Таблица.Ссылка.Наименование КАК Name
	     |ИЗ
	     |	Справочник.СтруктурныеЕдиницы.АйпиДляОпределенияТочкиПриПодключении КАК Таблица
	     |ГДЕ
	     |	Таблица.Айпи = &Айпи";
		 
	//
	ПЗ = Новый ПостроительЗапроса;
	ПЗ.Текст = ТЗ;
	
	//
	ПЗ.Параметры.Вставить("Айпи", СокрЛП(IP));
	
	//
	ПЗ.Выполнить();
	
	//
	МассивОтвет = Новый Массив;
	
	//
	Выборка = ПЗ.Результат.Выбрать();
	Пока Выборка.Следующий() Цикл
		
		//
		ТекстJSON = "{""ID"":""" + СокрЛП(Выборка.ID) + """, ""Name"":""" + СокрЛП(Выборка.Name) + """}";
		
	КонецЦикла;	
	
	//
	Возврат ТекстJSON;		 
	
КонецФункции	