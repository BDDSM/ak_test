
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ТабДокумент.Вывести(Параметры.ТабДок);
	ТабДокумент.ПолеСнизу = Параметры.ТабДок.ПолеСнизу;
	ТабДокумент.ПолеСверху = Параметры.ТабДок.ПолеСверху;
	ТабДокумент.ПолеСлева = Параметры.ТабДок.ПолеСлева;
	ТабДокумент.ПолеСправа = Параметры.ТабДок.ПолеСправа;
	ЭтаФорма.Заголовок = Параметры.УстановитьЗаголовок;
	Если Параметры.ОтображатьСетку Тогда
		Элементы.ТабДокумент.ОтображатьСетку = Истина;
		Элементы.ТабДокумент.ОтображатьЗаголовки = Истина;
	КонецЕсли;
	
	Если Параметры.РазрешитьРедактирование Тогда
		Элементы.ТабДокумент.Редактирование = Истина;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура Печать(Команда)
	
	ТабДокумент.Напечатать(РежимИспользованияДиалогаПечати.Использовать);
	
КонецПроцедуры
