
// процедура создает новый элемент справочника номенклатура и записывает его
//
&НаСервере
Процедура СоздатьНаСервере()
	НоменклатураОбъект = РеквизитФормыВЗначение("Объект");
	НоменклатураОбъект.Записать();
	ЗначениеВРеквизитФормы(НоменклатураОбъект,"Объект");
КонецПроцедуры

// Кнопка создает новый элемент справочника номенклатура, закрывает форму и возвращает ссылку на созданный элемент
//
&НаКлиенте
Процедура Создать(Команда)
	СоздатьНаСервере();
	Если ЗначениеЗаполнено(Объект.Ссылка) Тогда 
		ЭтаФорма.Закрыть(Объект.Ссылка);
	КонецЕсли;
КонецПроцедуры

// Процедура - При создании на сервере, заполняет реквизиты номенклатуры переданными параметрами
//
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Объект.Наименование = Параметры.Наименование;
	Объект.НаименованиеПолное = Параметры.Наименование;
	Объект.Услуга = Параметры.Услуга;
	Объект.ЕдиницаИзмерения = Параметры.ЕдиницаИзмерения;
КонецПроцедуры
