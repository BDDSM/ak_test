
// Кнопка создает новый элемент справочника номенклатура, закрывает форму и возвращает ссылку на созданный элемент
//
Процедура КнопкаВыполнитьНажатие(Кнопка)
	НоваяНоменклатура = Справочники.Номенклатура.СоздатьЭлемент();
	НоваяНоменклатура.Наименование = Наименование;
	НоваяНоменклатура.НаименованиеПолное = НаименованиеПолное;
	НоваяНоменклатура.Услуга = Услуга;
	НоваяНоменклатура.БазоваяЕдиницаИзмерения = БазоваяЕдиницаИзмерения;
	НоваяНоменклатура.Записать();
	ЭтаФорма.Закрыть(НоваяНоменклатура.Ссылка);
КонецПроцедуры
