
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	СтрукПараметры=Параметры.СтрукПараметры;
	Для каждого Эл Из СтрукПараметры Цикл
	    Попытка
		    ЭтаФорма[Эл.Ключ]=Эл.Значение;
		Исключение
		КонецПопытки;	
	КонецЦикла;
	
	ИндексыДокументов=ПолучитьСтруктуруИЗЦентральнойБазы("ИндексыДокументов");
	ДанныеДокументов=ПолучитьСтруктуруИЗЦентральнойБазы("ДанныеДокументов");

	СсылкаДок=ИндексыДокументов.Получить(Параметры.ИнДок);
	СтруктураДок=ДанныеДокументов.Получить(СсылкаДок);
	ТоварыКЗагрузке=СтруктураДок.ТабличныеЧасти.Товары;
	
	
	
	Для каждого Стр Из ТоварыКЗагрузке Цикл
		НовСтр=ОбработкаОбъект.Товары.Добавить();
	    ЗаполнитьЗначенияСвойств(НовСтр,Стр.Значение);
	КонецЦикла; 
КонецПроцедуры

&НаСервере
Функция ПолучитьСтруктуруИЗЦентральнойБазы(Ключ)
	Возврат ХранилищеОбщихНастроек.Загрузить(Ключ);
	

КонецФункции // ()
