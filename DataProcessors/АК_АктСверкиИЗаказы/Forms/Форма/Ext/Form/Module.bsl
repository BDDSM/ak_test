

&НаКлиенте
Процедура ВыполнитьКоманду(ИдентификаторКоманды) Экспорт
	
КонецПроцедуры	

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Если ТекущийРежимЗапуска() = РежимЗапускаКлиентскогоПриложения.УправляемоеПриложение Тогда
        ПараметрыФормы = Новый Структура(); 
        ОткрытьФорму(ПолучитьПолноеИмяФормы("ФормаАвторизацииУправляемая"), ПараметрыФормы, ЭтаФорма);
	Иначе
		//ПараметрыФормы = Новый Структура(); 
		//Ф=ПолучитьФорму(ПолучитьПолноеИмяФормы("ФормаАвторизацииОбычная"), ПараметрыФормы, ЭтаФорма);
		//Ф.ОткрытьМодально();
		ОткрытьФорму(ПолучитьПолноеИмяФормы("ФормаАвторизацииОбычная"));
	КонецЕсли;
	Отказ=Истина;
КонецПроцедуры

&НаКлиенте 
Функция ПолучитьПолноеИмяФормы(ИмяФормы)

    СимволТочка = ".";
    ПозицияТочки = СтрДлина(ЭтаФорма.ИмяФормы);
    Пока Сред(ЭтаФорма.ИмяФормы, ПозицияТочки, 1) <> СимволТочка Цикл ПозицияТочки = ПозицияТочки - 1; КонецЦикла; //
    Возврат Лев(ЭтаФорма.ИмяФормы, ПозицияТочки) + ИмяФормы;

КонецФункции

