
//+++АК LATV 2018.10.24 ИП-00020239
Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)

	Если ВыполняемоеДействие = "ЗаполнениеСкладов" Тогда
		
	ИначеЕсли ВыполняемоеДействие = "ЗаполнениеТТ" Тогда
		ПроверяемыеРеквизиты.Добавить("СкладыХранения");
		ПроверяемыеРеквизиты.Добавить("СкладыХранения.СкладХранения");
		
	ИначеЕсли ВыполняемоеДействие = "ИзменениеБлокировки" Тогда
		ПроверяемыеРеквизиты.Добавить("СкладыХранения");
		ПроверяемыеРеквизиты.Добавить("СкладыХранения.СкладХранения");
		ПроверяемыеРеквизиты.Добавить("ТорговыеТочки");
		
	КонецЕсли;

КонецПроцедуры
