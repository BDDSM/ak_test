
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	//Вставить содержимое обработчика
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	ИмяФайла = "\\10.0.0.80\e\Anketas\"+"00001"+".tif";	
	
	Если ПоместитьФайл(СсылкаНаКартинку, ИмяФайла, ИмяФайла, Ложь, ЭтаФорма.УникальныйИдентификатор) Тогда
		Элементы.СсылкаНаКартинку.Обновить();
	КонецЕсли;	
		
КонецПроцедуры

//&НаСервере
//Процедура УстановитьКартинкуНаСервере(АдресВременногоХранилища)
//	
//	Элементы.СсылкаНаКартинку.КартинкаЗначений = Новый Картинка(ПолучитьИзВременногоХранилища(АдресВременногоХранилища));
//	
//КонецПроцедуры	