
&НаКлиенте
Процедура Принять(Команда)
	
	СтатусОтказ = Ложь;
	Если Не ЗначениеЗаполнено(ПроцентСкидки) Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Не указан процент скидки",,,, СтатусОтказ);
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Комментарий) Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Не указан комменатрий",,,, СтатусОтказ);
	КонецЕсли;
	
	Если СтатусОтказ Тогда
		Возврат;
	КонецЕсли;	
	
	Закрыть(Новый Структура("Процент, Комментарий", ПроцентСкидки, Комментарий));
	
КонецПроцедуры