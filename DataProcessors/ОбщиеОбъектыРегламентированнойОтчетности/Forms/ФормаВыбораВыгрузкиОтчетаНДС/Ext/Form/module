
Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)
	
	ОтборДинамическогоСписка = ВыгрузкиРеглОтчетов.Отбор;
	
	ОтборДинамическогоСписка.Ссылка.Использование = Истина;
	ОтборДинамическогоСписка.Ссылка.ВидСравнения = ВидСравнения.ВСписке;
	ОтборДинамическогоСписка.Ссылка.Значение = ПарДекларацииНДС;
	
КонецПроцедуры

Процедура ОсновныеДействияФормыОК(Кнопка)
	
	Если ЭлементыФормы.ВыгрузкиРеглОтчетов.ТекущиеДанные = Неопределено Тогда
		Предупреждение("Выберите отчет для открытия!");
		Возврат;
	КонецЕсли;
	
	ВыбСтрока = ЭлементыФормы.ВыгрузкиРеглОтчетов.ТекущиеДанные.Ссылка;
	
	Закрыть(ВыбСтрока);
	
КонецПроцедуры

// Процедура - обработчик события Выбор табличного поля ВыгрузкиРеглОтчетов.
//
Процедура ВыгрузкиРеглОтчетовВыбор(Элемент, ВыбраннаяСтрока, Колонка, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	ВыбСтрока = ВыбраннаяСтрока;
	
	Закрыть(ВыбСтрока);
	
КонецПроцедуры
