// Параметры формы:
//   ИмяСохраняемогоПараметра - Строка - имя параметра, в котором будет сохраняться выбор пользователя
//   ЗначениеНеПоказывать - Произвольный - значение однозначно определяющее выбор пользователя
//       "больше не показывать" для конкретного уведомления.
//   РежимОткрытия - Строка - при значении "Принудительно" меняется состав кнопок формы
//   ТекстОписания - Текст HTML - текст уведомления
Перем ПараметрыФормы Экспорт;

Процедура ОсновныеДействияФормыБольшеНеПоказывать(Кнопка)
	
	СохранитьЗначение(ПараметрыФормы.ИмяСохраняемогоПараметра, ПараметрыФормы.ЗначениеНеПоказывать);
	ЭтаФорма.Закрыть();
	
КонецПроцедуры

Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)
	
	Если ПараметрыФормы.РежимОткрытия = "Принудительно" Тогда
		УдалитьКнопку("БольшеНеПоказывать");
		УдалитьКнопку("Разделитель");
		ЭлементыФормы.ОсновныеДействияФормы.Кнопки.ПоказатьПозже.Текст = "Закрыть";
	КонецЕсли;
	
	ЭлементыФормы.ПолеОписанияИзменений.УстановитьТекст(ПараметрыФормы.ТекстОписания);
	
КонецПроцедуры

Процедура УдалитьКнопку(ИмяКнопки)
	
	УдаляемаяКнопка = ЭлементыФормы.ОсновныеДействияФормы.Кнопки[ИмяКнопки];
	ЭлементыФормы.ОсновныеДействияФормы.Кнопки.Удалить(УдаляемаяКнопка);
	
КонецПроцедуры