//+++АК VERT 2018.08.14 Без номера, временная обработка, чтобы руками не делать
&НаСервере
Процедура УстановитьПризнакНаСервере()
	ОбработкаОбъект = РеквизитФормыВЗначение("Объект");
	ОбработкаОбъект.УстановитьПризнак(Истина);
КонецПроцедуры

//+++АК VERT 2018.08.14 Без номера, временная обработка, чтобы руками не делать
&НаКлиенте
Процедура УстановитьПризнак(Команда)
	Состояние("Установка признака");
	УстановитьПризнакНаСервере();
	Состояние("Признак установлен");
КонецПроцедуры

//+++АК VERT 2018.08.14 Без номера, временная обработка, чтобы руками не делать
&НаСервере
Процедура СнятьПризнакНаСервере()
	ОбработкаОбъект = РеквизитФормыВЗначение("Объект");
	ОбработкаОбъект.УстановитьПризнак(Ложь);
КонецПроцедуры

//+++АК VERT 2018.08.14 Без номера, временная обработка, чтобы руками не делать
&НаКлиенте
Процедура СнятьПризнак(Команда)
	Состояние("Снятие признака");
	СнятьПризнакНаСервере();
	Состояние("Признак снят");
КонецПроцедуры
