
Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)
	Период = НачалоМесяца(ДобавитьМесяц(ПолныеПрава.ТекущаяДатаСервера(), -1));
	СчётЗакрытия = ПланыСчетов.Финансовый.ЗатратыДляРаспределения;
КонецПроцедуры

Процедура КнопкаВыполнитьНажатие(Кнопка)
	Ответ = Вопрос("Распределить затраты?", РежимДиалогаВопрос.ДаНет);
	Если Ответ = КодВозвратаДиалога.Да Тогда
		ВыполнитьРаспределение(СтатьяДР);
		Предупреждение("Распределение затрат выполнено.");
	КонецЕсли;
КонецПроцедуры

Процедура ОсновныеДействияФормыДействиеУдалитьДвижения(Кнопка)
	Ответ = Вопрос("Все движения за указанный период будут удалены. Продолжить?", РежимДиалогаВопрос.ДаНет);
	Если Ответ = КодВозвратаДиалога.Да Тогда
		УдалитьДвиженияЗаПериод(СтатьяДР);
		Предупреждение("Удаление движений выполнено.");
	КонецЕсли;
КонецПроцедуры


