Процедура КнопкаВыполнитьНажатие(Кнопка)
	ПолучитьПочтуПоЗаявкам(); //Для тестирования	
КонецПроцедуры

Процедура КоманднаяПанель1ОткрытьЗаявкуДляТеста(Кнопка)
	Документ = Документы.ЗаявкаНаУслугиМатериалы.НайтиПоНомеру("000006498"); //Для тестирования
	Форма = Документ.ПолучитьФорму();
	Форма.Открыть();
КонецПроцедуры

Процедура ОтработатьПодтверждения(Кнопка)
	ОтработатьПодтвержденияСервер(); //Для тестирования
КонецПроцедуры

Процедура СнятьПодтверждениеНажатие(Элемент) //Для тестирования
	О = ЗаявкаНаУслугиМатериалы.ПолучитьОбъект();
	Для Каждого Строка ИЗ О.Услуги Цикл
		Строка.Акцептована = Ложь;
		Строка.Акцептовал = Неопределено;
		Строка.АкцептовалДата	= ТекущаяДата();
	КонецЦикла;
	О.Записать(РежимЗаписиДокумента.Запись);
КонецПроцедуры

Процедура ТекущийПользовательУстановитьНажатие(Элемент) //Для тестирования
	ПараметрыСеанса.ТекущийПользователь = ТекущийПользователь;
КонецПроцедуры

Процедура КоманднаяПанель1ТестированиеОтрабокти(Кнопка)
	ОтработатьПодтвержденияСерверТест();
КонецПроцедуры
