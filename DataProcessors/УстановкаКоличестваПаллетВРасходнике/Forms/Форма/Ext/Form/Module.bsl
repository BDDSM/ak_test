
&НаКлиенте
Процедура УстановитьКоличествоПаллет(Команда)
	Если Не ЗначениеЗаполнено(Объект.Расходник) Тогда
		Сообщить("Выберите расходник");	
		Возврат;
	КонецЕсли; 
	УстановитьКоличествоПаллетСервер()
КонецПроцедуры

&НаСервере
Процедура УстановитьКоличествоПаллетСервер()
	УстановитьПривилегированныйРежим(Истина);
	Док=Объект.Расходник.ПолучитьОбъект();
	Док.КоличествоПаллет=Объект.КоличествоПаллет;
	Док.Записать();
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Не РольДоступна("ПолныеПрава") И Не РольДоступна("ОператорСклада") И Не Строка(ПараметрыСеанса.ТекущийПользователь)="Кладовщик Кавказский" Тогда
		Сообщить("Недостаточно прав");	
		Отказ=Истина;
	
	КонецЕсли; 
КонецПроцедуры


