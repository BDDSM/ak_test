
Процедура КнопкаВыполнитьНажатие(Кнопка)
	ОбработатьТаблицуОшибокОтправкиПочтовыхСообщений(НесуществующиеАдреса.Выгрузить());	
КонецПроцедуры

Процедура КоманднаяПанель1Заполнить(Кнопка)
	НесуществующиеАдреса.Загрузить(ПолучитьТаблицуОшибокОтправкиПочтовыхСообщений(УчетнаяЗаписьЭлектроннойПочты, ДатаНачало, ДатаКонец));
КонецПроцедуры

Процедура ПриОткрытии()
	ДатаНачало = НачалоДня(ТекущаяДата());
	ДатаКонец = КонецДня(ТекущаяДата());
КонецПроцедуры

