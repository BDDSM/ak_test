
//+++АК LATV 2018.11.29 ИП-00020489
Функция КодДляВыполненияВПериодическихЗаданиях(ТекущаяОбработка, Параметры) Экспорт

	// Выполнение отчета
	ВыполняемаяОбработка = Обработки.ОчисткаУстаревшихФайловИзКаталогаХранения.Создать();
	
	Параметры.Свойство("Период",				ВыполняемаяОбработка.Период);
	Параметры.Свойство("СписанияНаМагазинах",	ВыполняемаяОбработка.СписанияНаМагазинах);
	
	ВыполняемаяОбработка.ОчиститьУстаревшиеФайлы();

КонецФункции
