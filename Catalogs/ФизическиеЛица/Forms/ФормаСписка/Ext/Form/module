
Процедура ДействияФормыПечатьБейджев(Кнопка)
	
	ТекДанные = ЭлементыФормы.СправочникСписок.ТекущиеДанные;
	Если ТекДанные = Неопределено Тогда
		Предупреждение("Нет выделенных строк для печати");
		Возврат;
	КонецЕсли;
	
	//
	МассивФизЛиц = Новый Массив;
	МассивФизЛиц.Добавить(ТекДанные.Ссылка);
	
	ТабДокумент = Обработки.ГрафикРаботыПродавцов_ТЗ.СформироватьТабличныйДокумент(МассивФизЛиц);
	
	ТабДокумент.ОтображатьСетку 	= Ложь;
	ТабДокумент.Защита 				= Истина;
	ТабДокумент.ТолькоПросмотр 		= Истина;
	ТабДокумент.ОтображатьЗаголовки = Ложь;
	ТабДокумент.Показать();
	
КонецПроцедуры

Процедура ДействияФормыСинхронизироватьСЗУП(Кнопка)
	
	
	
КонецПроцедуры
