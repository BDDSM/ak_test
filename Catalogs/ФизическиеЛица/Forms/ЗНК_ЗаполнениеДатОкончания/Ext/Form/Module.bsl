
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если НЕ Параметры.Свойство("Ссылка", Ссылка) Тогда 
		Сообщить("Данная форма доступна только для редактирования из справочника ""Физические лица""");
		Отказ = Истина;	
	КонецЕсли;
	
	Параметры.Свойство("ДатаОкончанияДействияАнализовПоМедКнижке", ДатаОкончанияДействияАнализовПоМедКнижке);	
	Параметры.Свойство("ДатаОкончанияДействияПсихологОсв", ДатаОкончанияДействияПсихологОсв);

КонецПроцедуры

&НаСервере
Процедура ЗаписатьФизЛицоНаСервере()
	
	УстановитьПривилегированныйРежим(Истина);
	
	ФизЛицоОбъект = Ссылка.ПолучитьОбъект();
	ФизЛицоОбъект.ДатаОкончанияДействияАнализовПоМедКнижке = ДатаОкончанияДействияАнализовПоМедКнижке;
	ФизЛицоОбъект.ДатаОкончанияДействияПсихологОсв = ДатаОкончанияДействияПсихологОсв;
	ФизЛицоОбъект.Записать();	
	
	УстановитьПривилегированныйРежим(Ложь);	
	
КонецПроцедуры

&НаКлиенте
Процедура Ок(Команда)
	
	ЗаписатьФизЛицоНаСервере();
	
	ЭтаФорма.Закрыть();
	
КонецПроцедуры
