
Процедура ПриЗаписи(Отказ)
	
	Запрос = Новый Запрос("ВЫБРАТЬ
	                      |	ЭДО_КаналыОбменаОрганизации.Организация
	                      |ИЗ
	                      |	Справочник.ЭДО_КаналыОбмена.Организации КАК ЭДО_КаналыОбменаОрганизации
	                      |ГДЕ
	                      |	ЭДО_КаналыОбменаОрганизации.Ссылка = &Ссылка
	                      |
	                      |СГРУППИРОВАТЬ ПО
	                      |	ЭДО_КаналыОбменаОрганизации.Организация
	                      |
	                      |ИМЕЮЩИЕ
	                      |	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ЭДО_КаналыОбменаОрганизации.НомерСтроки) > 1");
	
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	
	Если НЕ Запрос.Выполнить().Пустой() Тогда
		ОбщегоНазначения.СообщитьОбОшибке("Дублирование организаций в табличной части настройки обмена запрещено!", Отказ);
	КонецЕсли; 
	
КонецПроцедуры
