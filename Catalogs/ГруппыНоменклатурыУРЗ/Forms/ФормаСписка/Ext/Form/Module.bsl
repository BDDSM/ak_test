
Функция ПолучитьПараметрыОтбора(мГруппа)
	
	СтруктураОтбора = Новый Структура("ГруппаНоменклатурыУРЗ", мГруппа);
	
	Возврат Новый Структура("Отбор", СтруктураОтбора);
	
КонецФункции

&НаКлиенте
Процедура ВывестиНоменклатуруГруппы(Команда)
	
	ТекДанные = Элементы.Список.ТекущиеДанные;
	Если ТекДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ФормаСписка = ПолучитьФорму("Справочник.Номенклатура.ФормаСписка", ПолучитьПараметрыОтбора(ТекДанные.Ссылка));
	
	ФормаСписка.Элементы.Список.Отображение = ОтображениеТаблицы.Список;
	ФормаСписка.Заголовок = "Группа """ + СокрЛП(ТекДанные.Наименование) + """";	
	
	мЭлементыОтбора = ФормаСписка.Список.Отбор.Элементы;
	Для Каждого мЭлементОтбора Из мЭлементыОтбора Цикл
		Если мЭлементОтбора.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("ГруппаНоменклатурыУРЗ") Тогда
		    мЭлементОтбора.РежимОтображения = РежимОтображенияЭлементаНастройкиКомпоновкиДанных.Недоступный;
		КонецЕсли;
	КонецЦикла;

	
	ФормаСписка.ОткрытьМодально();
	
КонецПроцедуры
