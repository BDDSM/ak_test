

&НаКлиенте
Процедура КонтрагентПриИзменении(Элемент)
	
	Если ЗначениеЗаполнено(Объект.Контрагент) Тогда
		Объект.Наименование = Объект.Контрагент;
	Иначе
		Объект.Наименование = "<Соответствие не установлено>";
	КонецЕсли; 

КонецПроцедуры

