&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	Если НЕ ЗначениеЗаполнено(ПараметрКоманды) Тогда
		ОбщегоНазначения.СообщитьИнформациюПользователю("Элемент справочника не записан! Для печати необходимо записать элемент справочника!");
		Возврат;
	КонецЕсли;
	ТД=Печать(ПараметрКоманды);
	ТД.Показать();
КонецПроцедуры

&НаСервере
Функция Печать(ПараметрКоманды)
	Возврат Справочники.Склады.ПечатьШК(ПараметрКоманды);
КонецФункции

