
&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	
	Если НЕ ЗначениеЗаполнено(Объект.Наименование) Тогда
		Объект.Наименование = Формат(Объект.Коэффициент1, "ЧРГ=") + "; " + 
								Строка(Объект.Коэффициент2) + "; " + 
								Строка(Объект.Коэффициент3) + "; " + 
								Строка(Объект.Коэффициент4) + "; " + 
								Строка(Объект.Коэффициент5); 
	КонецЕсли;
	
КонецПроцедуры
