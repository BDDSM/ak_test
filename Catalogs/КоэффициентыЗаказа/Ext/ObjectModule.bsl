
Процедура ПередЗаписью(Отказ)
	
	Если НЕ ЗначениеЗаполнено(ЭтотОбъект.Наименование) Тогда
		ЭтотОбъект.Наименование = Строка(ЭтотОбъект.Коэффициент1) + "; " + 
									Строка(ЭтотОбъект.Коэффициент2) + "; " + 
									Строка(ЭтотОбъект.Коэффициент3) + "; " + 
									Строка(ЭтотОбъект.Коэффициент4) + "; " + 
									Строка(ЭтотОбъект.Коэффициент5); 
	КонецЕсли;
	
КонецПроцедуры
