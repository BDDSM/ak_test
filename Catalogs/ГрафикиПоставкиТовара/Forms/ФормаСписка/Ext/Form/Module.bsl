
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ЕстьДоступКоВсемРасчетчикам = ДопМодульСервер.ПолучитьЗначениеПраваПользователя("ПрогнозированиеРазрешитьДоступКоВсемРасчетчикам", Ложь);
	
	Если НЕ ЕстьДоступКоВсемРасчетчикам Тогда
		
		СтандартнаяОбработка = Ложь;
	
		ТекРасчетчик = ДопМодульСервер.ПолучитьЗначениеПраваПользователя("ПрогнозированиеРасчетчик", Справочники.Расчетчики.ПустаяСсылка());
		
		ОтборНаФорме = ЭтаФорма.Список.Отбор;
		
		ЭлементОтбора = ОтборНаФорме.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ЭлементОтбора.ЛевоеЗначение 	= Новый ПолеКомпоновкиДанных("Расчетчик");
		ЭлементОтбора.Использование 	= Истина;
		ЭлементОтбора.ВидСравнения 		= ВидСравненияКомпоновкиДанных.Равно;
		ЭлементОтбора.ПравоеЗначение 	= ТекРасчетчик;
    	ЭлементОтбора.РежимОтображения 	= РежимОтображенияЭлементаНастройкиКомпоновкиДанных.Недоступный;
		
		Элементы.Список.Обновить();
		
	КонецЕсли;
	
КонецПроцедуры
