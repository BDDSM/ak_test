&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	Приемник=ОткрытьФормуМодально("Справочник.Пользователи.ФормаВыбора");
	Если ЗначениеЗаполнено(Приемник) Тогда
		Попытка
			КопироватьНастройкиСервер(ПараметрКоманды,Приемник);
			Сообщить("Операция успешно выполнена");
		Исключение
			Сообщить("Операция не выполнена. "+ОписаниеОшибки());
		КонецПопытки; 
	КонецЕсли; 
КонецПроцедуры

&НаСервере
Функция КопироватьНастройкиСервер(ПараметрКоманды,Приемник)
	Справочники.Пользователи.КопироватьДопНастройки(ПараметрКоманды,Приемник);
КонецФункции

