Процедура ОтобразитьСостояние(ТекстСообщения) Экспорт
		
	#Если Клиент Тогда
	Состояние(ТекстСообщения);
	#КонецЕсли

КонецПроцедуры

Процедура ПроверитьПрерывание() Экспорт
	#Если Клиент Тогда
	ОбработкаПрерыванияПользователя();
	#КонецЕсли
КонецПроцедуры	

Функция ОткрытьФормуХодаОбработки() Экспорт

	#Если Клиент Тогда
		возврат ПолучитьФорму("ОбщаяФорма.CMS1C_ХодВыполненияОбработкиДанных");
	#Иначе
		Структура1 = Новый Структура("МаксимальноеЗначение");
		Структура2 = Новый Структура("Диаграмма",Структура1);
		Структура3 = Новый Структура("Элементы,Диаграмма,НаименованиеОбработкиДанных,КомментарийОбработкиДанных,КомментарийЗначения",Структура2,0,"","","");
		
		
		
		
		возврат Структура3;
	#КонецЕсли

КонецФункции

Функция ПолучитьФормуЗагрузкиЗаказов() Экспорт
	
	возврат ПолучитьФорму("ФормаЗагрузкиЗаказов");
	
КонецФункции