
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Объект.Ссылка.Пустая() Тогда
		ТекПользователь = ПараметрыСеанса.ТекущийПользователь;
		Объект.Контрагент = ТекПользователь.КонтрагентАутсорсинг;
	КонецЕсли;
	
КонецПроцедуры
