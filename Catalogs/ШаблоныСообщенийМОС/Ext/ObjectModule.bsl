
Процедура ПередЗаписью(Отказ)
	
	ТипСообщенияСтр = ОбщегоНазначения.ПолучитьИмяЗначенияПеречисленияПоСсылке(ТипСообщения);
	
	Если Предопределенный Тогда
		ИмяПредопределенного = Справочники.ШаблоныСообщенийМОС.ПолучитьИмяПредопределенного(Ссылка);
	Иначе
		ИмяПредопределенного = "";
	КонецЕсли;
	
	Если НЕ ПрименяетсяВМагазине И НЕ ПрименяетсяВЦентре Тогда
		Сообщить("Установите хотя бы один флажок применимости");
		Отказ = Истина;
		Возврат;
	КонецЕсли;
	
	Если НЕ ПрименяетсяВМагазине Тогда
		ОбменДанными.Получатели.Очистить(); 
	КонецЕсли; 

КонецПроцедуры
