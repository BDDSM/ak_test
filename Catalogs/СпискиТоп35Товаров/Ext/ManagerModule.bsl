
Процедура СформироватьОписаниеЭлемента(ТабДок, Ссылка) Экспорт
	
	Если ЗначениеЗаполнено(Ссылка) Тогда
		Запрос = Новый Запрос();
		Запрос.УстановитьПараметр("Ссылка", Ссылка);
		Запрос.Текст = "ВЫБРАТЬ
		               |	СпискиТоп35ТоваровТовары.Номенклатура,
		               |	СпискиТоп35ТоваровТовары.Номенклатура.Наименование КАК Наименование,
		               |	СпискиТоп35ТоваровТовары.Состав КАК Состав,
		               |	СпискиТоп35ТоваровТовары.СрокГодности КАК СрокГодности,
		               |	СпискиТоп35ТоваровТовары.Описание КАК Описание
		               |ИЗ
		               |	Справочник.СпискиТоп35Товаров.Товары КАК СпискиТоп35ТоваровТовары
		               |ГДЕ
		               |	СпискиТоп35ТоваровТовары.Ссылка = &Ссылка
		               |
		               |УПОРЯДОЧИТЬ ПО
		               |	СпискиТоп35ТоваровТовары.НомерСтроки";
					   
		Макет = Справочники.СпискиТоп35Товаров.ПолучитьМакет("МакетВыводаЭлемента");
		
		Область = Макет.ПолучитьОбласть("Шапка");
		Область.Параметры.ДатаНач = Формат(Ссылка.ДатаНачалаДействия, "ДФ=dd.MM.yyyy");
		Область.Параметры.ДатаКон = Формат(Ссылка.ДатаОкончанияДействия, "ДФ=dd.MM.yyyy");
		ТабДок.Вывести(Область);
		
		Выборка = Запрос.Выполнить().Выбрать();
		Пока Выборка.Следующий() Цикл
			Область = Макет.ПолучитьОбласть("Строка");
			Область.Параметры.Заполнить(Выборка);
			ТабДок.Вывести(Область);
		КонецЦикла;	
	КонецЕсли;	
	
КонецПроцедуры	
