
Процедура ПриОткрытии()
	Если  РольДоступнаСервер("Финансист")или  РольДоступнаСервер("ПолныеПрава") Тогда
	
	Иначе
		ЭтаФорма.ТолькоПросмотр = Истина;	
	КонецЕсли; 	
КонецПроцедуры


&НаСервере
Функция РольДоступнаСервер(Роль)

	
Возврат РольДоступна(Роль);
КонецФункции // ()
