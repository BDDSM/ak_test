
Процедура ПередЗаписью(Отказ)
	Если НЕ ЗначениеЗаполнено(ПредставлениеТаблицы1С) Тогда
		ПредставлениеТаблицы1С = Таблица1С;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ПредставлениеТаблицыСайта) Тогда
		ПредставлениеТаблицыСайта = ТаблицаСайт;
	КонецЕсли;
	
	Если Наименование = "" Тогда
		Наименование = СгенерироватьНаименованиеСопоставления();
	КонецЕсли;
КонецПроцедуры

Функция СгенерироватьНаименованиеСопоставления()
	Стр = "";
	Стр = Стр + ПредставлениеТаблицы1С;
	//Если Загрузка Тогда
	//	Стр = Стр + "<";
	//КонецЕсли;
	//
	//Если СопоставлениеПередОбменом Тогда
	//	Стр = Стр + "=";
	//КонецЕсли;
	//
	//Если Выгрузка Тогда
	//	Стр = Стр + ">";
	//КонецЕсли;
	
	Стр = Стр + " <--> ";
	
	Стр = Стр + ПредставлениеТаблицыСайта;
	Возврат СТр;
КонецФункции

