
&НаКлиенте
Процедура ПроектПриИзменении(Элемент)
	Если Объект.Проект Тогда
		Объект.Устарел = ЛОЖЬ;
	КонецЕсли;	
КонецПроцедуры

&НаКлиенте
Процедура УстарелПриИзменении(Элемент)
	Если Объект.Устарел Тогда
		Объект.Проект = ЛОЖЬ;
	КонецЕсли;	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Не ЗначениеЗаполнено(Объект.Ссылка) Тогда
		Объект.Проект = Истина;
	КонецЕсли;	
КонецПроцедуры
