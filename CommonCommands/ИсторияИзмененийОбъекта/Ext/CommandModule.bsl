
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	#Если ТонкийКлиент ИЛИ ВебКлиент Тогда
		ОткрытьФорму("Отчет.ИсторияИзмененийОбъектов.Форма.ФормаОтчетаУпр", Новый Структура("СсылкаНаОбъект, ЗаполнятьСписокВерсий", ПараметрКоманды, Истина));
	#Иначе	
		ИсторияИзменений = Отчеты.ИсторияИзмененийОбъектов.Создать();
		ИсторияИзменений.СсылкаНаОбъект = ПараметрКоманды;
		                   
		Форма = ИсторияИзменений.ПолучитьФорму("ФормаОтчета");
		Форма.СформироватьТаблицуВерсий();
		Форма.Открыть();
	#КонецЕсли
	
КонецПроцедуры

