//+++АК ILIK 2018.07.19 ИП-00019130
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Не РольДоступна("ПолныеПрава")
	   И Не УправлениеДопПравамиПользователей.ПолучитьПравоПользователяУпр(ПланыВидовХарактеристик.ПраваПользователей.АкцептантЗаявкиДляМедицины, Ложь) Тогда
		Если Документы.ЗаявкаНаКомпенсациюСпортаИМедицины.ЭтоРуководительПодразделения() Тогда
			Акцептант = ПараметрыСеанса.ТекущийПользователь;
			Элементы.ОтборАкцептант.ТолькоПросмотр = Истина;
		ИначеЕсли УправлениеДопПравамиПользователей.ПолучитьПравоПользователяУпр(ПланыВидовХарактеристик.ПраваПользователей.СогласующийТилНет, Ложь) Тогда
			ВидЗаявки = Перечисления.ВидыЗаявокНаКомпенсацию.Медицина;
			Элементы.ОтборВидЗаявки.ТолькоПросмотр = Истина;
		Иначе
			Ответственный = ПараметрыСеанса.ТекущийПользователь;
			Элементы.ОтборОтветственный.ТолькоПросмотр = Истина;
		КонецЕсли;
	КонецЕсли;
	
	УстановитьОтборы();
КонецПроцедуры

//+++АК ILIK 2018.07.19 ИП-00019130
&НаСервере
Процедура УстановитьОтборы()
	Список.Параметры.УстановитьЗначениеПараметра("Акцептант", Акцептант);
	Список.Параметры.УстановитьЗначениеПараметра("Ответственный", Ответственный);
	Список.Параметры.УстановитьЗначениеПараметра("ВидЗаявки", ВидЗаявки);
КонецПроцедуры

//+++АК ILIK 2018.07.19 ИП-00019130
&НаКлиенте
Процедура ОтборОтветственныйПриИзменении(Элемент)
	УстановитьОтборы();
КонецПроцедуры

//+++АК ILIK 2018.07.19 ИП-00019130
&НаКлиенте
Процедура ОтборВидЗаявкиПриИзменении(Элемент)
	УстановитьОтборы();
КонецПроцедуры

//+++АК ILIK 2018.07.19 ИП-00019130
&НаКлиенте
Процедура ОтборАкцептантПриИзменении(Элемент)
	УстановитьОтборы();
КонецПроцедуры

//+++АК ILIK 2018.07.19 ИП-00019130
&НаКлиенте
Процедура ОтборАкцептантНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	
	ПараметрыОткрытия = Новый Структура("Отбор", Новый Структура("Ссылка", ПолучитьСписокРуководителей()));
	ОткрытьФорму("Справочник.Пользователи.ФормаВыбора", ПараметрыОткрытия, Элемент);
КонецПроцедуры

//+++АК ILIK 2018.07.19 ИП-00019130
&НаСервереБезКонтекста
Функция ПолучитьСписокРуководителей()
	Возврат Документы.ЗаявкаНаКомпенсациюСпортаИМедицины.ПолучитьСписокРуководителей();
Конецфункции