
Процедура ПриЗаписи(Отказ)
	
	Если ЭтотОбъект.ЭтоНовый() Тогда
		Возврат;
	КонецЕсли;
	
	ТекстЗапроса = "";
	
КонецПроцедуры

Процедура ПриКопировании(ОбъектКопирования)
	
	ЭтотОбъект.Вопросы.Очистить();
	//+++АК POZM 2018.10.16 ИП-00020081 
	ЭтотОбъект.ВидеоЗаписи.Очистить();
	//---АК POZM 
КонецПроцедуры

Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	
	Если ЭтотОбъект.ЭтоНовый() Тогда
		ЭтотОбъект.Автор = ПараметрыСеанса.ТекущийПользователь;
	КонецЕсли;	
	
	ВерныеСтроки = ЭтотОбъект.Вопросы.НайтиСтроки(Новый Структура("ПравильныйОтвет", Истина));
	
	ЭтотОбъект.Результат = ВерныеСтроки.Количество();
	//+++АК POZM 2018.04.25 ИП-00018499
	НеверныхСтрок = ЭтотОбъект.Вопросы.Количество()-ЭтотОбъект.Результат;
	//---АК POZM 
	Если ЭтотОбъект.Вопросы.Количество()>0 Тогда
		//+++АК POZM 2018.04.25 ИП-00018499
		//ЭтотОбъект.ПроцентВерныхОтветов = ЭтотОбъект.Результат/ЭтотОбъект.Вопросы.Количество()*100;
		ЭтотОбъект.ПроцентВерныхОтветов = 100-4*НеверныхСтрок;
		//---АК POZM 
	КонецЕсли;	
КонецПроцедуры
