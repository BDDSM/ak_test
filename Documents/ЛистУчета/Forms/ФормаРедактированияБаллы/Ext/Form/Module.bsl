
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Данные.Загрузить(ЗначениеИзСтрокиВнутр(Параметры.ТаблицаДанные));
	
КонецПроцедуры

&НаКлиенте
Процедура Принять(Команда)
	
	МассивСтроки = Новый Массив();
	Для Каждого СтрокаТаб Из Данные Цикл
		МассивСтроки.Добавить(Новый Структура("Терминал, Сумма", СтрокаТаб.Терминал, СтрокаТаб.Сумма));
	КонецЦикла;	
	
	Закрыть(МассивСтроки);
	
КонецПроцедуры
