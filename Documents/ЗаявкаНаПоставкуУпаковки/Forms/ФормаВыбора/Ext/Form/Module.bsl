
&НаКлиенте
Процедура СписокПОСОткрытие(Элемент, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	ОткрытьЗначение(СписокПОС);
	
	
КонецПроцедуры

Процедура ОбновитьСписокПриходныхОрдеров()

	ТД1 = Элементы.Список.ТекущаяСтрока;
	Если ТД1 = Неопределено Тогда
		СписокПОС.Очистить();
		ПриходныеОрдераСклад = "";
	Иначе
		СписокПОС = Документы.ЗаявкаНаПоставкуУпаковки.ПолучитьСЗПриходныхОрдеровСклад(ТД1.Ссылка);
		ПриходныеОрдераСклад = Строка(СписокПОС);
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура СписокПриАктивизацииСтроки(Элемент)
	
	ОбновитьСписокПриходныхОрдеров();
	
КонецПроцедуры
