
&НаКлиенте
Процедура ОК(Команда)
	ОповеститьОВыборе(Новый Структура("КоличествоЧастей",?(КоличествоЧастей<ВсегоСтрок,КоличествоЧастей,1)));
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
	Закрыть(0);
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Параметры.Свойство("ВсегоСтрок") Тогда
		ВсегоСтрок = Параметры.ВсегоСтрок;
	КонецЕсли;
КонецПроцедуры
