
&НаКлиенте
Процедура ВыборПериода(Команда)
	
	Перем Диалог;
	
	Диалог = Новый ДиалогРедактированияСтандартногоПериода();
	
	Диалог.Период.ДатаНачала    = ЭтаФорма.ДатаНачала;
	Диалог.Период.ДатаОкончания = ЭтаФорма.ДатаОкончания;
	
	Если Диалог.Редактировать() Тогда
		
		ЭтаФорма.ДатаНачала		= Диалог.Период.ДатаНачала;
		ЭтаФорма.ДатаОкончания	= Диалог.Период.ДатаОкончания;
	
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьПоМаршруту(Команда)
	
	СтруктураПараметров = Новый Структура("ДатаНачала, ДатаОкончания", ЭтаФорма.ДатаНачала, ЭтаФорма.ДатаОкончания);
	
	ЭтаФорма.Закрыть(СтруктураПараметров);
	
КонецПроцедуры
