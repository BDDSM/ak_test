
Функция ПечатьТовары(ПараметрКоманды)
	
	Возврат Документы.МаршрутныйЛист.ПечатьРасходныйОрдер_Товары(ПараметрКоманды);
	
КонецФункции

Функция ПечатьОборудование(ПараметрКоманды)
	
	Возврат Документы.МаршрутныйЛист.ПечатьРасходныйОрдер_Оборудование(ПараметрКоманды);
	
КонецФункции

Функция ПроверитьСервер(ПараметрКоманды)
	
	Возврат (НЕ ПараметрКоманды.ВидПеревозки = Справочники.АК_ВидыПеревозки.ДоставкаНаТТ);
	
КонецФункции

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	Если ПроверитьСервер(ПараметрКоманды) Тогда
		Сообщить("Печать расходных ордеров не предусмотрена для данного вида перевозки!");
		Возврат;
	КонецЕсли;
	
	// для товаров
	ТабДокумент = ПечатьТовары(ПараметрКоманды);

	Если НЕ ТабДокумент = Неопределено Тогда
		ОповеститьОбИзменении(ПараметрКоманды);
	
		ТабДокумент.Показать("Печать расходных ордеров");
	КонецЕсли;
	
	// для оборудования
	ТабДокумент = ПечатьОборудование(ПараметрКоманды);

	Если НЕ ТабДокумент = Неопределено Тогда
		ОповеститьОбИзменении(ПараметрКоманды);
	
		ТабДокумент.Показать("Печать расходных ордеров");
	КонецЕсли;
	
КонецПроцедуры
