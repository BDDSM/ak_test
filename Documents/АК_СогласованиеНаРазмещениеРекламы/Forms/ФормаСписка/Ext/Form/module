
Процедура ДокументСписокПриПолученииДанных(Элемент, ОформленияСтрок)
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ РАЗЛИЧНЫЕ
	               |	АК_СогласованиеНаРазмещениеРекламы.ТорговаяТочка КАК ТорговаяТочка
	               |ИЗ
	               |	Документ.АК_СогласованиеНаРазмещениеРекламы КАК АК_СогласованиеНаРазмещениеРекламы
	               |ГДЕ
	               |	АК_СогласованиеНаРазмещениеРекламы.ПометкаУдаления = ЛОЖЬ
	               |	И (АК_СогласованиеНаРазмещениеРекламы.Бессрочное = ИСТИНА
	               |			ИЛИ АК_СогласованиеНаРазмещениеРекламы.Бессрочное = ЛОЖЬ
	               |				И АК_СогласованиеНаРазмещениеРекламы.СрокКонец <> ДАТАВРЕМЯ(1, 1, 1)
	               |				И АК_СогласованиеНаРазмещениеРекламы.СрокКонец >= &МинимальнаяДатаНачалаУведомления)";
 	Запрос.УстановитьПараметр("МинимальнаяДатаНачалаУведомления", ДобавитьМесяц(ТекущаяДата(), 2));
 	ДействующиеТТ = Запрос.Выполнить().Выгрузить();;
 
	Для Каждого ОформлениеСтроки Из ОформленияСтрок Цикл
		ДействующаяТТ = Ложь;
		Если ДействующиеТТ.Найти(ОформлениеСтроки.ДанныеСтроки.ТорговаяТочка, "ТорговаяТочка") <> Неопределено Тогда
			ДействующаяТТ = Истина;
		КонецЕсли;
		
		//Если (ЗначениеЗаполнено(ОформлениеСтроки.ДанныеСтроки.СрокКонец)
		//	И ОформлениеСтроки.ДанныеСтроки.СрокКонец < ДобавитьМесяц(ТекущаяДата(), 2))
		//	И ДействующаяТТ = Ложь)
		//	ИЛИ
		Если ЗначениеЗаполнено(ОформлениеСтроки.ДанныеСтроки.СрокКонец)
			И ОформлениеСтроки.ДанныеСтроки.СрокКонец < ТекущаяДата()
			Тогда
			ОформлениеСтроки.ЦветТекста = WebЦвета.СветлоСерый;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ОформлениеСтроки.ДанныеСтроки.СрокКонец)
			И ОформлениеСтроки.ДанныеСтроки.СрокКонец < ДобавитьМесяц(ТекущаяДата(), 2)
			И ОформлениеСтроки.ДанныеСтроки.СрокКонец >= ТекущаяДата()
			И ДействующаяТТ = Ложь Тогда
			ОформлениеСтроки.ЦветФона = WebЦвета.ПесочноКоричневый;
		КонецЕсли;
	
	КонецЦикла;
	
	
КонецПроцедуры

Процедура ДействияФормыПолучателиУведомлений(Кнопка)
	
	ПолучитьФорму("РегистрСведений.АК_СогласованиеПоРекламеПолучателиУведомлений.ФормаСписка").Открыть();
	
КонецПроцедуры
