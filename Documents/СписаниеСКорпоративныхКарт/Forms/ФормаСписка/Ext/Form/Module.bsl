
&НаКлиенте
Процедура ВыгрузитьВБухгалтерию(Команда)
	
	ТекДанные = Элементы.Список.ТекущиеДанные;
	Если ТекДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ФормаОбработки = ПолучитьФорму("Обработка.НастройкаВыгрузкиПлатёжныхДокументовВБП.Форма",, Элементы.Список, ЭтаФорма.КлючУникальности);
	ФормаОбработки.ОбработкаОбъект.ВыгружатьСписаниеСКорпоративныхКарт = Истина;
	
	ФормаОбработки.Открыть();
	
КонецПроцедуры
