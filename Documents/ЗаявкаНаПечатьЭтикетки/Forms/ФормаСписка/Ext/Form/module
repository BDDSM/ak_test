
Процедура ДокументСписокПриПолученииДанных(Элемент, ОформленияСтрок)
	
	//МассивЗаявок = Новый Массив;
	//
	//Для Каждого ЭлОформления ИЗ ОформленияСтрок Цикл
	//	МассивЗаявок.Добавить(ЭлОформления.ДанныеСтроки.Ссылка);		
	//КонецЦИкла;
	//
	//Выборка = ПолучитьСписокВведенныхОрдеров(МассивЗаявок);
	//
	//Для Каждого ЭлОформления Из ОформленияСтрок Цикл
	//	
	//	Если ЭлОформления.ДанныеСтроки.ВыполненЗаказ Тогда
	//		//ЭлОформления.ЦветТекста = WebЦвета.Серый;
	//		ЭлОформления.ЦветФона = Новый Цвет(236,204,206);
	//	КонецЕсли;
	//	
	//	Если Выборка.НайтиСледующий(Новый Структура("Заявка", ЭлОформления.ДанныеСтроки.Ссылка)) Тогда
	//		ВыборкаОрдера = Выборка.Выбрать();
	//		
	//		ТекстОрдер = "";
	//		
	//		Пока ВыборкаОрдера.Следующий() Цикл
	//			ТекстОрдер = ТекстОрдер + Строка(ВыборкаОрдера.Ордер) + ",";
	//		КонецЦикла;
	//		
	//		Если ЗначениеЗаполнено(ТекстОрдер) Тогда
	//			текстОрдер = Лев(ТекстОрдер, Стрдлина(ТекстОрдер) - 1);
	//		КонецЕсли;
	//		
	//		ЭлОформления.Ячейки.ВведеныОрдера.ОтображатьТекст = Истина;
	//		ЭлОформления.Ячейки.ВведеныОрдера.Текст = ТекстОрдер;
	//		
	//		Выборка.Сбросить();
	//		
	//	КонецЕсли;
	//	
	//КонецЦикла;	
	
КонецПроцедуры

Функция ПолучитьСписокВведенныхОрдеров(МассивЗаявок)
	
	//Запрос = Новый Запрос;
	//Запрос.Текст = "ВЫБРАТЬ
	//			   |	РасходныйОрдерСклад.Ссылка КАК Ордер,
	//			   |	РасходныйОрдерСклад.Основание КАК Заявка
	//			   |ИЗ
	//			   |	Документ.РасходныйОрдерСклад КАК РасходныйОрдерСклад
	//			   |ГДЕ
	//			   |	РасходныйОрдерСклад.Основание В(&МассивЗаявок)
	//			   |ИТОГИ ПО
	//			   |	Заявка";
	//			   
	//Запрос.УстановитьПараметр("МассивЗаявок", МассивЗаявок);
	//
	//Возврат Запрос.Выполнить().Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);	
	
КонецФункции

// Процедура вызывается при выборе пункта подменю "Структура подчиненности" меню "Перейти".
Процедура СтруктураПодчиненностиДокумента(Кнопка)
	
	Если ЭлементыФормы.ДокументСписок.ТекущиеДанные = Неопределено тогда
		Возврат
	КонецЕсли;
	
	РаботаСДиалогами.ПоказатьСтруктуруПодчиненностиДокумента(ЭлементыФормы.ДокументСписок.ТекущиеДанные.Ссылка);
	
КонецПроцедуры


