
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	//Вставить содержимое обработчика.
	//ПараметрыФормы = Новый Структура("", );
	//ОткрытьФорму("Документ.АК_ОбращениеВТехподдержку.ФормаСписка", ПараметрыФормы, ПараметрыВыполненияКоманды.Источник, ПараметрыВыполненияКоманды.Уникальность, ПараметрыВыполненияКоманды.Окно);
	
	//+++АК SHEP 20160510: 
	// Если на момент входящего звонка 1С не запущена, тогда при следующем открытии выводить все новые записи, которые были приняты во время закрытого 1С
	Состояние("Получаем отсутствующие в 1С обращения в тех. поддержку...");
	МассивСтруктурДляДокумента = АК_ТелефонияСервер.ПодготовитьМассивСтруктурПоОтсутствующимОбращениямТехподдержки();
	Для Каждого СтруктураДокумента Из МассивСтруктурДляДокумента Цикл
		ОткрытьФорму("Документ.АК_ОбращениеВТехподдержку.ФормаОбъекта", Новый Структура("Основание", СтруктураДокумента), , СтруктураДокумента.UniqueID);
	КонецЦикла;
	//---АК SHEP 20160510
	
КонецПроцедуры
