
&НаКлиенте
Процедура Сгруппировать(Команда)
	Объект.ВременныеИнтервалы.Сортировать("ВременнойИнтервал");
КонецПроцедуры


&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	//+++АК sils 08.06.2018 ИП-00018876
	ОперацияАпдекс = APDEX_ОценкаПроизводительностиКлиентСервер.ПолучитьОперацию("Открытие документа Квота на доставку товара на ТТ");
	APDEX_ОценкаПроизводительностиКлиентСервер.НачатьЗамерВремени(ОперацияАпдекс);
	//---АК
	Контрагент=ПараметрыСеанса.ТекущийПользователь.ФизЛицо.Контрагент;
	Если Контрагент<>Объект.Подрядчик И ЗначениеЗаполнено(Контрагент) И РольДоступна("Перевозчик") Тогда
		Сообщить("Этот документ относится к другой организации");
		Отказ=Истина;
	КонецЕсли; 
	//Контрагент=ПараметрыСеанса.ТекущийПользователь.ФизЛицо.Контрагент;
	//Если ЗначениеЗаполнено(Контрагент) Тогда
	//	Элементы.ВременныеИнтервалы.ОтборСтрок=Новый ФиксированнаяСтруктура("Подрядчик", Контрагент);
	//КонецЕсли; 
КонецПроцедуры

//+++АК sils 08.06.2018 ИП-00018876
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	APDEX_ОценкаПроизводительностиКлиентСервер.ЗакончитьЗамерВремени(ОперацияАпдекс, ?(Параметры.Ключ.Пустая(), "Новый документ", "" + Объект.Ссылка));
КонецПроцедуры
                                                                                    