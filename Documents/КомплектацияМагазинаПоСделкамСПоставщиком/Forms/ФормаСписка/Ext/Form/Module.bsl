
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Список.Параметры.УстановитьЗначениеПараметра("ТекущаяДата", ТекущаяДата());
КонецПроцедуры

&НаКлиенте
Процедура ОтборМагазинНаименованиеПриИзменении(Элемент)
	
	ЭлементыОтбора = ЭтаФорма.Список.Отбор.Элементы;
	Для Каждого ЭлементОтбора Из ЭлементыОтбора Цикл
		
		Если СокрЛП(ЭлементОтбора.ЛевоеЗначение) = "Магазин.Наименование" Тогда
			Если ЗначениеЗаполнено(ОтборМагазинНаименование) Тогда
				ЭлементОтбора.Использование 	= ИСтина;
				ЭлементОтбора.ВидСравнения 		= ВидСравненияКомпоновкиДанных.Содержит;
				ЭлементОтбора.ПравоеЗначение 	= ОтборМагазинНаименование;
			Иначе
				ЭлементОтбора.Использование 	= Ложь;
			КонецЕсли;	
		КонецЕсли;
    КонецЦикла;
				
	
КонецПроцедуры

