
&НаКлиенте
Процедура Свернуть(Команда)
	Элементы.Группа3.Видимость = Ложь;
	Элементы.Свернуть.Видимость = Ложь;
	Элементы.Развернуть.Видимость = Истина;
КонецПроцедуры

&НаКлиенте
Процедура Развернуть(Команда)
	Элементы.Группа3.Видимость = Истина;
	Элементы.Свернуть.Видимость = Истина;
	Элементы.Развернуть.Видимость = Ложь;
КонецПроцедуры

&НаСервере
Процедура ПередатьДанныеНаСервере()
	ПараметрыЗаполнения = Новый Структура;
	
	ФабрикаXD = ОбменССистемойВетис.ПолучитьФабрикуXDTOApplicationManagements();
	НастройкаAPI = ОбменССистемойВетис.ОсновнаяНастройкаАПИ();
	ПараметрыПодключения = ОбменССистемойВетис.ПолучитьПараметрыПодключения(ФабрикаXD, НастройкаAPI);
	
	ПараметрыФункции = ОбменССистемойВетис.ПолучитьПараметрыФункции("ResolveDiscrepancyOperation", ФабрикаXD, НастройкаAPI);
	stockDiscrepancy = ОбменССистемойВетис.ПолучитьОбъектXDTO(ФабрикаXD, "vetd:StockDiscrepancy");
КонецПроцедуры

&НаКлиенте
Процедура ПередатьДанные(Команда)
	ПередатьДанныеНаСервере();
КонецПроцедуры
