
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	табличныйДокумент = печать(параметрКоманды);
	табличныйДокумент.отображатьСетку = ложь;
	табличныйДокумент.отображатьЗаголовки = ложь;
	табличныйДокумент.ключПараметровПечати = "РАСПОРЯЖЕНИЕ_НА_ЛИМИТЫ";
	табличныйДокумент.показать();
КонецПроцедуры

&наСервере
функция печать(параметрКоманды) 
	возврат документы.УстановкаЛимитовКассы.печать(параметрКоманды);
конецФункции