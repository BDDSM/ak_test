
&НаКлиенте
Процедура ИзменитьИнвентарныеНомера(Команда)
	
	ТекДанные = Элементы.Список.ТекущиеДанные;
	Если ТекДанные = Неопределено Тогда
		Возврат;	
	КонецЕсли;
	
	СтруктураПараметров = Новый Структура;
	СтруктураПараметров.Вставить("ДокументСсылка", ТекДанные.Ссылка);
	ОткрытьФорму("Документ.ПоступлениеОС.Форма.ФормаИзмененияИнвентарныхНомеровУпр", СтруктураПараметров);
	
КонецПроцедуры
