//+++AK GREK 28.07.2018 ИП-00019390
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	Период = ТекущаяДата();
	ПараметрыВыбора = Новый Структура("НачалоПериода, КонецПериода", НачалоМесяца(Период), КонецМесяца(Период));
	ОписаниеОповещения = Новый ОписаниеОповещения("ВыбратьПериодЗавершение", ЭтотОбъект);
	ОткрытьФорму("ОбщаяФорма.ВыборСтандартногоПериодаМесяц", ПараметрыВыбора, ЭтотОбъект, , , , ОписаниеОповещения);
КонецПроцедуры

//+++AK GREK 28.07.2018 ИП-00019390
&НаКлиенте
Процедура ВыбратьПериодЗавершение(Результат, Параметры) Экспорт	
    Если Не Результат = Неопределено Тогда
        ПечатныеФормы = Печать(Результат);
		
		ПечатныеФормы.Стр1.ОтображатьСетку = Ложь;
		ПечатныеФормы.Стр1.ОтображатьЗаголовки = Ложь;
		ПечатныеФормы.Стр1.Показать();
		
		ПечатныеФормы.Стр2.ОтображатьСетку = Ложь;
		ПечатныеФормы.Стр2.ОтображатьЗаголовки = Ложь;
		ПечатныеФормы.Стр2.Показать();
    КонецЕсли;
КонецПроцедуры 

//+++AK GREK 28.07.2018 ИП-00019390
&НаСервере
Функция Печать(Период)
	Возврат Документы.ВозвратТоваровОтПокупателя.ПечатьСписанияТовараПоВозвратамОтX5(Период);
КонецФункции
