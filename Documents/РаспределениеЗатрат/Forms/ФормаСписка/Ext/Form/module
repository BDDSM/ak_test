Процедура ДействияФормыПроводкиДтКт(Кнопка)

	Если ЭлементыФормы.ДокументСписок.ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	БухгалтерскийУчет.ОткрытьЖурналПроводок(ЭлементыФормы.ДокументСписок.ТекущиеДанные.Ссылка);

КонецПроцедуры

Процедура ДействияФормыРаспределитьВкусвиллОбщий(Кнопка)
	
	Документы.РаспределениеЗатрат.ПолучитьФорму("ФормаРаспределенияВкусвиллОбщий").Открыть();
	
КонецПроцедуры

//+++ ZICD 13.02.2017 ИП-00014919
Процедура ДействияФормыДействиеЗакрыть44ыйСчёт(Кнопка)
	ФормаОбработки = Обработки.РаспределениеЗатрат.ПолучитьФорму("Форма");
	Если ФормаОбработки.Открыта() Тогда
		ФормаОбработки.Активизировать();
	Иначе
		ФормаОбработки.Открыть();
	КонецЕсли;
КонецПроцедуры
//---
