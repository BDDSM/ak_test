
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	//Если НЕ ПолучитьПризкакПроведенности(ПараметрКоманды) Тогда
	//	ОбщегоНазначения.СообщитьИнформациюПользователю("Документ не проведен! Для печати необходимо провести документ!");
	//	Возврат;
	//КонецЕсли;
	
	ТабДок = ПечатьНакладной(ПараметрКоманды);
	
	ТабДок.ОтображатьСетку 		= Ложь;
	ТабДок.ТолькоПросмотр 		= Истина;
	ТабДок.ОтображатьЗаголовки 	= Ложь;
	ТабДок.Показать();
	
КонецПроцедуры

&НаСервере
Функция ПолучитьПризкакПроведенности(СсылкаНаОбъект)
	
	Возврат СсылкаНаОбъект.Проведен;
	
КонецФункции

&НаСервере
Функция ПечатьНакладной(СсылкаНаОбъект)
	
	ДокументОбъект = СсылкаНаОбъект.ПолучитьОбъект();
	Если ДокументОбъект = Неопределено Тогда Возврат Новый ТабличныйДокумент; КонецЕсли;
	
	Возврат ДокументОбъект.ПечатьНакладной();
	
КонецФункции
