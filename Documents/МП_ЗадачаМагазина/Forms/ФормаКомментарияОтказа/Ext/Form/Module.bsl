
&НаКлиенте
Процедура Принять(Команда)
	
	Закрыть(Новый Структура("Комментарий, КолвоОтказов", Комментарий, КолвоОтказов));
	
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
	
	Закрыть();
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	КолвоОтказов = Параметры.КолвоОтказов + 1;
	Если КолвоОтказов > 2 Тогда
		КолвоОтказов = 2;
	КонецЕсли;
	
	Комментарий = Параметры.Комментарий;
	
КонецПроцедуры
