
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	//Вставить содержимое обработчика.
	//Если ПровереноОператором(ПараметрКоманды) Тогда
		ТабДок = Печать(ПараметрКоманды);

		ТабДок.ОтображатьСетку = Ложь;
		ТабДок.Защита = Истина;
		ТабДок.ТолькоПросмотр = Истина;
		ТабДок.ОтображатьЗаголовки = Ложь;
		ТабДок.Показать();
	//Иначе
	//	Предупреждение("Накладную М11 можно распечатать только после проверки оператором",);
	//КонецЕсли
КонецПроцедуры

&НаСервере
Функция Печать(ПараметрКоманды)
	Возврат Документы.ЗаявкаНаСклад.ПечатьМ11(ПараметрКоманды);
КонецФункции

&НаСервере
Функция ПровереноОператором(ПараметрКоманды)
	Возврат ПараметрКоманды.ПровереноОператором;
КонецФункции