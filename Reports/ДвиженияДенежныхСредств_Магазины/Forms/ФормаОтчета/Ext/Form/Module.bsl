
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	КомпоновщикНастроекКомпоновкиДанных = Отчет.КомпоновщикНастроек;
	ПН = КомпоновщикНастроекКомпоновкиДанных.ПользовательскиеНастройки;
	
	ЭлементОтбора = КомпоновщикНастроекКомпоновкиДанных.Настройки.ПараметрыДанных.Элементы.Найти("Период");
	Элемент = ПН.Элементы.Найти(ЭлементОтбора.ИдентификаторПользовательскойНастройки);
	Элемент.Значение = Новый СтандартныйПериод(НачалоДня(ТекущаяДата()), КонецДня(ТекущаяДата()));
	Элемент.Использование = Истина;
	
	//Для Каждого ПользПоле Из Отчет.КомпоновщикНастроек.ПользовательскиеНастройки.Элементы Цикл
	//	Если ТипЗнч(ПользПоле) = Тип("ЗначениеПараметраНастроекКомпоновкиДанных")
	//			И Строка(ПользПоле.Параметр) = "Период" Тогда
	//		ПользПоле.Значение.ДатаНачала = НачалоДня(ТекущаяДата());
	//		ПользПоле.Значение.ДатаОкончания = КонецДня(ТекущаяДата());
	//		Прервать;
	//	КонецЕсли;	
	//КонецЦикла;
	
	//ПараметрПериод = Отчет.КомпоновщикНастроек.ПользовательскиеНастройки.Элементы.Найти("Период");
	//Если ПараметрПериод = Неопределено Тогда
	//	ПараметрПериод = Отчет.КомпоновщикНастроек.ПользовательскиеНастройки.Элементы.Добавить(Тип("ЗначениеПараметраНастроекКомпоновкиДанных"));
	//	ПараметрПериод.Значение.ДатаНачала = НачалоДня(ТекущаяДата());
	//	ПараметрПериод.Значение.ДатаОкончания = КонецДня(ТекущаяДата());
	//КонецЕсли;	
	
КонецПроцедуры
