
&НаКлиенте
Процедура РабочаяБазаПриИзменении(Элемент)
	
	Отчет.ТестоваяБаза = НЕ Отчет.РабочаяБаза;
	
КонецПроцедуры

&НаКлиенте
Процедура ТестоваяБазаПриИзменении(Элемент)
	
	Отчет.РабочаяБаза = НЕ Отчет.ТестоваяБаза;
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Отчет.РабочаяБаза = Истина;
	Отчет.СерверРабочейБазы = "srv-sql02";
	Отчет.СерверТестовойБазы = "srv-sql02";
	Отчет.ИмяБД = "BP_corp";
	Отчет.ИмяТестовойБД = "BP_CORP_deb";
	
КонецПроцедуры

//&НаКлиенте
//Процедура ЗагрузитьНастройки(Команда)
//	
//	Диалог = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Открытие);
//	
//	Диалог.Фильтр = "Файлы настроек отчетов *.rep1c|*.rep1c";
//	
//	Если НЕ Диалог.Выбрать() Тогда
//		Возврат;
//	КонецЕсли;
//	
//	ТекстовыйФайл = Новый ТекстовыйДокумент;
//	ТекстовыйФайл.Прочитать(Диалог.ПолноеИмяФайла);
//	
//	Текст = ТекстовыйФайл.ПолучитьТекст();
//	
//	ЗагрузитьНастройкиСервер(Текст);
//	
//КонецПроцедуры

//&НаСервере
//Процедура ЗагрузитьНастройкиСервер(Текст)
//		
//	СтруктураНастроек = ЗначениеИзСтрокиВнутр(Текст);
//	
//	Если ТипЗнч(СтруктураНастроек) <> Тип("Структура") Тогда
//		Возврат;
//	КонецЕсли;

//	
//	Об = РеквизитФормыВЗначение("Отчет");
//	Об.ЗагрузитьНастройкиМодуль(СтруктураНастроек);
//	ЗначениеВРеквизитФормы(Об, "Отчет");
//	
//КонецПроцедуры
//	
