
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	//ПараметрыДанных = Отчет.КомпоновщикНастроек.Настройки.ПараметрыДанных.Элементы;            
	//СтатьяПараметр = ПараметрыДанных.Найти("СтатьяТД"); 
	//СтатьяПараметр.Значение = Справочники.СтатьиТовародвижения.НайтиПоНаименованию("Доставка товаров на склад");
	//
	СразуСформироватьОтчет = Параметры.СразуСформироватьОтчет;
	////ПолучитьФорму("Отчет.АК_РасшифровкаКБДРДоставкаТовараНаСклад.Форма.ФормаОтчета", );
	//
	Если СразуСформироватьОтчет = Истина Тогда
		НачалоПериода = Параметры.НачалоПериода;
		КонецПериода = Параметры.КонецПериода;
	//	
	//	Настройка = Отчет.КомпоновщикНастроек.ПолучитьНастройки();
	//	ПериодПараметр1 = Настройка.ПараметрыДанных.Элементы.Найти("Период");
	//	ПериодПараметр1.Значение.ДатаНачала = НачалоДня(НачалоПериода);
	//	ПериодПараметр1.Значение.ДатаОкончания = КонецДня(КонецПериода);

	//	ПараметрыДанных = Отчет.КомпоновщикНастроек.Настройки.ПараметрыДанных.Элементы;            
	//	ПериодПараметр = ПараметрыДанных.Найти("Период"); 
	//	ПериодПараметр.Значение.ДатаНачала = НачалоДня(НачалоПериода);
	//	ПериодПараметр.Значение.ДатаОкончания = КонецДня(КонецПериода);
	//	//МойПараметр.Значение = Строка("%"+Преподаватель+"%");            
	//	//МойПараметр.Использование = Истина;  
	//	//
	//  
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Если СразуСформироватьОтчет Тогда
		//ПараметрыДанных = Отчет.КомпоновщикНастроек.Настройки.ПараметрыДанных.Элементы;  
		//ПериодПараметр = ПараметрыДанных.Найти("Период"); 
		//ПериодПараметр.Значение = Новый СтандартныйПериод;
		//ПериодПараметр.Значение.ДатаНачала = НачалоДня(НачалоПериода);
		//ПериодПараметр.Значение.ДатаОкончания = КонецДня(КонецПериода);	
		//ПериодПараметр.Использование = Истина;
		//
		//Настройка = Отчет.КомпоновщикНастроек.ПолучитьНастройки();
		//ПериодПараметр1 = Настройка.ПараметрыДанных.Элементы.Найти("Период");
		//ПериодПараметр1.Значение = Новый СтандартныйПериод;
		//ПериодПараметр1.Значение.ДатаНачала = НачалоДня(НачалоПериода);
		//ПериодПараметр1.Значение.ДатаОкончания = КонецДня(КонецПериода);
		//ПериодПараметр1.Использование = Истина;
		
		СтатьяТД = Отчет.КомпоновщикНастроек.ПользовательскиеНастройки.Элементы.Найти(Отчет.КомпоновщикНастроек.Настройки.ПараметрыДанных.НайтиЗначениеПараметра(Новый ПараметрКомпоновкиДанных("СтатьяТД")).ИдентификаторПользовательскойНастройки);
	//СтатьяТД.Значение = Справочники.СтатьиТовародвижения.НайтиПоНаименованию("Доставка товаров на склад");	

		
		ПользовательскиеНастройки = Отчет.КомпоновщикНастроек.ПользовательскиеНастройки;
		
		ПараметрПериода = Отчет.КомпоновщикНастроек.ПользовательскиеНастройки.Элементы.Найти(Отчет.КомпоновщикНастроек.Настройки.ПараметрыДанных.НайтиЗначениеПараметра(Новый ПараметрКомпоновкиДанных("Период")).ИдентификаторПользовательскойНастройки);
		ПараметрПериода.Значение.ДатаНачала = НачалоДня(НачалоПериода);
		ПараметрПериода.Значение.ДатаОкончания = КонецДня(КонецПериода);
	    ПараметрПериода.Использование = Истина;
		
		СкомпоноватьРезультат();
	КонецЕсли;	
		
КонецПроцедуры
